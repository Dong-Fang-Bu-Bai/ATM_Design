# ATM 系统第二次迭代记录

## 1. 本次迭代目标

依据 `ATM_四人分工与三次迭代任务.md` 和 `ATM_UML作业方案.md`，第二次迭代目标主要包括：

- 完成取款、存款、转账、修改密码等核心业务功能
- 完善账户信息查询、会话校验和交易前校验
- 实现交易详情查询，形成主要业务闭环
- 初步完成前后端真实联调所需接口契约
- 保持 OpenAPI、前端调用、后端 DTO 和手工接口测试示例一致

## 2. 本次完成范围

### 2.1 前端页面与交互

前端已完成第二次迭代核心业务页面：

- `WithdrawView.vue`：取款页面
- `DepositView.vue`：存款页面
- `TransferView.vue`：转账页面
- `ChangePasswordView.vue`：修改密码页面

主菜单已接入取款、存款、转账、修改密码入口。前端请求统一通过 `frontend/src/api/atm.js` 发起，并支持真实后端模式和 mock 演示模式。

### 2.2 认证与账户模块

后端认证与账户模块已补充：

- 修改密码接口：`POST /api/atm/auth/change-password`
- 完整账户信息接口：`GET /api/atm/accounts/full-info`
- 交易前校验接口：`GET /api/atm/accounts/validate-transaction`
- 会话校验工具：通过 `SessionValidator` 统一校验 `sessionId`
- 统一响应结构：`code`、`message`、`data`、`timestamp`

### 2.3 交易模块

交易模块已从第一次迭代的接口骨架推进到真实业务实现：

- 取款接口：校验金额、100 整数倍、单次限额、单日限额和账户余额
- 存款接口：校验金额和单次存款限额，更新账户余额
- 转账接口：校验目标账户、余额、自转账、单次限额和单日限额，并在事务中完成扣款、入账和交易记录写入
- 交易详情接口：根据 `transactionId` 查询单笔交易详情
- 交易记录表：通过 `transaction_record` 保存交易流水数据

## 3. 公开字段统一结果

本次统一的是公开接口契约和文档字段，不强制删除实体层内部扩展字段。

| 类型 | 统一字段或路径 | 说明 |
| --- | --- | --- |
| 会话字段 | `sessionId` | 前端、OpenAPI、HTTP 示例和新增文档统一使用 |
| 兼容字段 | `token` | 仅作为后端兼容旧请求的别名，不作为新文档主字段 |
| 账户路径 | `/api/atm/accounts/*` | 账户公开接口统一使用复数路径 |
| 兼容账户路径 | `/api/atm/account/*` | 仅作为后端兼容路径保留 |
| 交易流水号 | `transactionId` | 对外返回和查询交易详情时统一使用 |
| 转账目标账户 | `targetAccountNo` | 转账请求中的目标账户字段 |
| 内部扩展字段 | `targetBank` | 仅保留在实体和数据库层用于后续扩展，不作为当前公开请求字段 |

交易请求字段统一如下：

| 功能 | 请求字段 |
| --- | --- |
| 取款 | `sessionId`, `amount`, `printReceipt` |
| 存款 | `sessionId`, `amount`, `printReceipt` |
| 转账 | `sessionId`, `targetAccountNo`, `amount`, `printReceipt` |
| 修改密码 | `sessionId`, `oldPassword`, `newPassword` |
| 交易前校验 | query 参数 `sessionId` |
| 交易详情 | path 参数 `transactionId` |

## 4. 当前接口清单

| 功能 | 方法 | 路径 |
| --- | --- | --- |
| 登录 | `POST` | `/api/atm/auth/login` |
| 退出登录 | `POST` | `/api/atm/auth/logout` |
| 修改密码 | `POST` | `/api/atm/auth/change-password` |
| 基础账户信息 | `GET` | `/api/atm/accounts/profile?sessionId=...` |
| 完整账户信息 | `GET` | `/api/atm/accounts/full-info?sessionId=...` |
| 查询余额 | `GET` | `/api/atm/accounts/balance?sessionId=...` |
| 交易前校验 | `GET` | `/api/atm/accounts/validate-transaction?sessionId=...` |
| 取款 | `POST` | `/api/atm/transactions/withdraw` |
| 存款 | `POST` | `/api/atm/transactions/deposit` |
| 转账 | `POST` | `/api/atm/transactions/transfer` |
| 交易详情 | `GET` | `/api/atm/transactions/{transactionId}` |

## 5. 文档与示例同步

本次同步了以下公开材料：

- `openapi-atm.yaml`：交易接口已删除第一次迭代遗留的 `501` 骨架说明，并按当前实现补充 `400`、`401`、`404` 响应描述
- `atm-server-auth/src/api-test.http`：补充登录后取款、存款、转账、交易详情、余额不足和目标账户不存在的手工测试链路
- `README.md`：补充第二次迭代交易接口和交易请求字段表
- `atm-server-auth/FUNCTIONS.md`：补充交易接口和统一字段说明
- `Iteration2-Frontend-Review.md`：去除真实交易接口仍为骨架的过时描述
- `transaction/交易模块设计文档.md`：明确 `targetBank` 为内部扩展字段，不属于当前公开请求契约

## 6. 验证结果

后端测试命令：

```powershell
cd .\atm-server-auth
.\mvnw.cmd test
```

验证结果：

```text
Tests run: 13, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

新增交易服务集成测试覆盖：

- 成功取款和存款后余额更新、交易详情可查
- 余额不足时拒绝交易，并保持账户余额不被错误扣减
- 目标账户不存在时拒绝转账，并保持源账户余额不变
- 成功转账后源账户扣款、目标账户入账，交易详情记录 `targetAccountNo`

## 7. 尚未纳入本次迭代范围

以下内容仍建议放在第三次迭代或最终集成阶段处理：

- 交易流水列表页面与分页查询接口实现
- 凭条展示、凭条查询和打印相关能力
- ATM 设备状态、吐钞能力检查和设备异常处理
- UML 中期/终稿材料的集中整理与图像化输出
- 前端构建 chunk size warning 优化

## 8. 阶段结论

第二次迭代已经完成 ATM 系统核心业务闭环。系统目前具备登录、账户查询、余额查询、修改密码、取款、存款、转账和交易详情查询能力，前端页面、后端接口、OpenAPI、HTTP 示例和主要说明文档已经统一到同一套公开契约。

本轮统一后的公开字段为 `sessionId`、`transactionId`、`targetAccountNo`，账户路径统一使用 `/api/atm/accounts/*`。旧字段 `token` 和旧路径 `/api/atm/account/*` 仅作为后端兼容别名保留，不再作为新文档和前端主调用口径。
