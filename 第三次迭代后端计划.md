# ATM 后端第三次迭代计划与完成记录

## 1. 迭代依据

第三次迭代依据 `总体迭代计划.png` 中“第四章 细化迭代3”的要求，结合 `ATM_四人分工与三次迭代任务.md`、`ATM_UML作业方案.md`、`Iteration3-Frontend-Review.md` 和 `openapi-atm.yaml` 制定。后端侧重点不是重新设计核心交易，而是在第二次迭代已完成登录、账户、取款、存款、转账和交易详情的基础上，补齐最终演示所需的流水、凭条、设备状态、吐钞检查、异常处理和集成测试。

## 2. 后端迭代目标

- 完成交易流水查询接口，支持当前会话账户分页查询。
- 完成交易凭条查询接口，按 `transactionId + sessionId` 限定凭条访问范围。
- 完成 ATM 设备状态接口，返回设备编号、位置、运行状态和可用现金。
- 完成吐钞能力检查接口，供前端在取款前判断设备现金是否充足。
- 将设备现金与真实取款流程联动，取款成功后同步扣减设备可用现金。
- 继续保持公开契约一致：`sessionId`、`transactionId`、`targetAccountNo`、`/api/atm/accounts/*`。
- 补充后端测试和接口示例，保证最终构建和核心功能链路可验证。

## 3. 接口计划

| 功能 | 方法 | 路径 | 请求口径 | 响应核心字段 |
| --- | --- | --- | --- | --- |
| 交易流水 | `GET` | `/api/atm/transactions/history` | `sessionId`, `page`, `size` | `page`, `size`, `total`, `records` |
| 交易凭条 | `GET` | `/api/atm/receipts/{transactionId}` | `transactionId`, `sessionId` | `transactionId`, `type`, `amount`, `balanceAfter`, `time`, `accountNo` |
| 设备状态 | `GET` | `/api/atm/device/status` | 无 | `atmCode`, `location`, `status`, `cashAvailable` |
| 吐钞检查 | `POST` | `/api/atm/device/cash-check` | `amount` | `available`, `amount`, `cashAvailable` |

## 4. 实现范围

本次后端实现按现有工程风格完成：

- 新增 `atm_device` 演示设备表和初始化数据。
- 新增 `AtmDevice` 实体、`AtmDeviceMapper`、`DeviceService`、`DeviceController`。
- 扩展 `TransactionMapper`，支持按账户分页查询交易流水和统计总数。
- 扩展 `TransactionService`，提供交易流水查询和凭条查询。
- 新增 `ReceiptController`，对外提供凭条查询接口。
- 调整取款流程，在余额校验通过后增加设备现金校验，取款成功时扣减 ATM 可用现金。
- 补充控制器测试和服务集成测试，覆盖第三次迭代新增接口、会话账户隔离、设备现金扣减和异常分支。

## 5. 验收口径

第三次迭代后端验收按以下结果判断：

- `mvnw.cmd test` 能通过全部后端测试。
- `GET /api/atm/transactions/history` 仅返回当前 `sessionId` 对应账户的交易记录。
- `GET /api/atm/receipts/{transactionId}` 必须携带当前账户的 `sessionId`，不能跨账户查看凭条。
- `GET /api/atm/device/status` 能返回初始化设备 `ATM001` 的运行状态。
- `POST /api/atm/device/cash-check` 能根据请求金额返回设备现金是否充足。
- 取款成功后账户余额和设备可用现金同步减少，存款和转账不改变设备现金。
- README、HTTP 示例和 OpenAPI 规范与真实后端实现保持一致。

## 6. 验证结果

已执行后端测试：

```powershell
cd .\atm-server-auth
.\mvnw.cmd test
```

结果：构建成功，全部测试通过。
