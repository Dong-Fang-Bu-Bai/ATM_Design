# ATM 系统第三次迭代记录

## 1. 本次迭代目标

依据 `ATM_四人分工与三次迭代任务.md`、`ATM_UML作业方案.md` 和 `总体迭代计划.png`，第三次迭代主要面向系统最终演示与报告收尾，目标包括：

- 完成交易流水查询、交易凭条查询和 ATM 设备状态相关功能
- 在取款流程中补充吐钞能力检查和设备现金扣减
- 完善前端最终演示入口，将流水、凭条和设备状态纳入主菜单
- 继续保持公开契约一致，避免前后端字段和路径分叉
- 补充第三次迭代测试和构建验证，保证最终演示链路可复现
- 为最终报告中的细化迭代 3、系统集成和部署说明提供事实依据

## 2. 本次完成范围

### 2.1 前端页面与交互

前端在第二次迭代核心交易页面基础上，补充了第三次迭代最终演示所需页面：

- `TransactionHistoryView.vue`：交易流水页面，支持按当前会话账户分页查看交易记录
- `ReceiptView.vue`：交易凭条页面，支持从交易结果或流水记录进入，也支持手动输入交易编号查询
- `DeviceStatusView.vue`：设备状态页面，展示 ATM 编号、位置、运行状态和可用现金，并提供吐钞能力检查

主菜单已新增“交易流水”“交易凭条”“设备状态”入口。取款、存款、转账成功后均提供“查看凭条”操作，余额页也补充了进入交易流水的入口。

前端接口封装集中在 `frontend/src/api/atm.js`，新增或接入了以下方法：

- `getTransactionHistory(sessionId, params)`
- `getReceipt(transactionId, sessionId)`
- `getDeviceStatus()`
- `checkCashAvailability(payload)`

前端 mock 模式同步补充了交易流水、凭条、设备状态和吐钞能力检查行为，可在不启动后端时完成第三次迭代演示流程。

### 2.2 后端交易、凭条与设备模块

后端在第二次迭代已完成取款、存款、转账和交易详情的基础上，继续补齐最终演示需要的交易记录和设备能力：

- 扩展 `TransactionService`，支持交易流水分页查询
- 新增凭条查询能力，通过 `transactionId + sessionId` 限定访问范围
- 新增 `ReceiptController`，提供 `/api/atm/receipts/{transactionId}` 接口
- 新增 `AtmDevice` 实体、`AtmDeviceMapper`、`DeviceService` 和 `DeviceController`
- 新增 `atm_device` 演示设备表，并初始化设备 `ATM001`
- 新增设备状态接口和吐钞能力检查接口
- 在取款流程中增加设备现金校验，取款成功后同步扣减 ATM 可用现金
- 保持存款和转账不改变设备现金，避免设备现金与账户余额逻辑混淆

交易流水和凭条查询均基于当前会话账户进行约束，避免跨账户查看交易凭条。

### 2.3 认证、账户与异常处理

第三次迭代继续沿用第二次迭代统一后的会话与账户口径：

- 会话主字段继续使用 `sessionId`
- `token` 仅作为后端兼容字段，不作为新文档和前端主字段
- 账户接口主路径继续使用 `/api/atm/accounts/*`
- 修改密码成功后当前会话失效，需要重新登录
- 交易和设备异常通过 `ApiException` 与 `GlobalExceptionHandler` 转换为统一响应结构

交易异常处理进一步覆盖余额不足、目标账户不存在、取款金额不符合规则、超过限额、ATM 现金不足和设备不可用等分支。

## 3. 公开契约延续与新增接口

第三次迭代继续保持以下公开契约：

| 类型 | 统一字段或路径 | 说明 |
| --- | --- | --- |
| 会话字段 | `sessionId` | 前端、OpenAPI、HTTP 示例和新增文档统一使用 |
| 兼容字段 | `token` | 仅作为后端兼容旧请求的别名 |
| 账户路径 | `/api/atm/accounts/*` | 账户公开接口主路径 |
| 交易流水号 | `transactionId` | 交易详情、流水和凭条查询统一使用 |
| 转账目标账户 | `targetAccountNo` | 转账请求中的目标账户字段 |

本次新增或重点完成的第三次迭代接口如下：

| 功能 | 方法 | 路径 | 请求字段 | 响应重点 |
| --- | --- | --- | --- | --- |
| 交易流水 | `GET` | `/api/atm/transactions/history` | `sessionId`, `page`, `size` | `page`, `size`, `total`, `records` |
| 交易凭条 | `GET` | `/api/atm/receipts/{transactionId}` | `transactionId`, `sessionId` | `transactionId`, `type`, `amount`, `balanceAfter`, `time`, `accountNo` |
| 设备状态 | `GET` | `/api/atm/device/status` | 无 | `atmCode`, `location`, `status`, `cashAvailable` |
| 吐钞检查 | `POST` | `/api/atm/device/cash-check` | `amount` | `available`, `amount`, `cashAvailable` |

`openapi-atm.yaml`、`README.md`、`atm-server-auth/FUNCTIONS.md` 和 `atm-server-auth/src/api-test.http` 已包含上述第三次迭代接口。

## 4. 前后端联调与演示流程

当前推荐真实联调演示流程如下：

1. 启动后端和前端真实联调模式。
2. 使用演示卡号 `6222020000000001` 和密码 `123456` 登录。
3. 在主菜单查看设备状态提示，进入设备状态页执行吐钞能力检查。
4. 执行取款、存款或转账交易。
5. 交易成功后进入凭条页面查看单笔交易凭条。
6. 进入交易流水页面查看当前账户分页交易记录，并从流水列表再次进入凭条。
7. 如需验证修改密码，应放在交易、流水和凭条演示之后执行；修改密码成功后当前 `sessionId` 会失效。
8. 演示结束后退卡退出。

该流程覆盖了第三次迭代要求中的流水、凭条、设备状态、吐钞检查、异常提示和最终页面串联。

## 5. 验证结果

### 5.1 后端验证

后端测试命令：

```powershell
cd .\atm-server-auth
.\mvnw.cmd test
```

验证结果：

```text
Tests run: 19, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

测试覆盖重点包括：

- 成功取款和存款后账户余额更新，交易详情可查询
- 取款成功后设备现金同步扣减
- 存款和转账不影响设备现金
- 余额不足时拒绝取款并保持账户余额不被错误扣减
- 目标账户不存在时拒绝转账并保持源账户余额不变
- 成功转账后源账户扣款、目标账户入账
- 交易流水仅返回当前 `sessionId` 对应账户的交易记录
- 凭条查询必须匹配当前会话账户，不能跨账户查看
- 设备状态和吐钞能力检查能够反映初始化设备 `ATM001` 的现金状态

### 5.2 前端验证

前端构建命令：

```powershell
cd .\frontend
npm run build
```

验证结果：

```text
vite build completed
```

构建过程中仍存在 Vite 的 chunk size warning，主要来自 Element Plus 体积较大。该 warning 不影响第三次迭代功能演示和生产构建通过，后续如需优化可考虑按需引入组件或配置 `manualChunks`。

## 6. 文档与示例同步

第三次迭代相关材料已经形成以下支撑：

- `第三次迭代后端计划.md`：记录后端第三次迭代目标、接口计划、实现范围、验收口径和测试结果
- `Iteration3-Frontend-Review.md`：记录前端第三次迭代页面、接口、mock、构建验证和当前结论
- `README.md`：补充完整运行方式、第三次迭代接口清单和推荐演示顺序
- `openapi-atm.yaml`：补充交易流水、凭条、设备状态和吐钞检查接口定义
- `atm-server-auth/src/api-test.http`：补充第三次迭代接口的手工验证请求
- `atm-server-auth/FUNCTIONS.md`：同步认证、账户、交易、凭条和设备接口说明

## 7. 最终报告可引用表述

第三次迭代在第二次迭代核心交易闭环基础上，重点补齐最终演示和报告收尾所需的交易流水、交易凭条、设备状态与吐钞能力检查功能。系统通过交易记录表保存取款、存款和转账产生的业务流水，并在凭条查询中结合当前会话限制访问范围，避免不同账户之间的交易信息泄露。设备模块通过 `atm_device` 表维护 ATM 终端编号、位置、运行状态和可用现金，取款流程在扣减账户余额前会检查设备现金是否充足，并在取款成功后同步扣减设备可用现金。

前端在本轮新增交易流水、交易凭条和设备状态页面，并将这些页面接入主菜单和交易结果页，使用户可以从登录、设备检查、交易办理、凭条查看、流水查询到退卡退出形成完整演示路径。前后端继续统一使用 `sessionId`、`transactionId`、`targetAccountNo` 和 `/api/atm/accounts/*` 作为公开契约，OpenAPI、README 和 HTTP 示例与真实实现保持一致。

经过后端测试和前端构建验证，第三次迭代已经具备课程作业最终演示所需的主要功能闭环。后续最终报告中可在 UML 终稿、部署说明和答辩材料部分继续补充状态图、组件图、部署图及演示截图，使文档交付与代码实现进一步对应。

## 8. 尚需最终报告整理的内容

以下内容不影响第三次迭代功能闭环，但建议在最终提交前继续整理：

- 将 `ATM_UML作业方案.md` 中的状态图、组件图、部署图集中整理为最终报告中的第四章材料
- 如需与前两次迭代保持一致，可补充 `docs/iteration3-*` 目录和对应 Mermaid 渲染图片
- 整理答辩 PPT 或最终说明材料，明确第三次迭代完成的流水、凭条、设备和测试验证内容
- 如需优化前端构建体积，可处理 Element Plus 引起的大 chunk warning

## 9. 阶段结论

第三次迭代已经完成 ATM 系统最终演示所需的主要功能补齐。系统目前具备登录、账户查询、余额查询、修改密码、取款、存款、转账、交易详情、交易流水、交易凭条、设备状态和吐钞能力检查能力，前端页面、后端接口、OpenAPI、HTTP 示例和主要说明文档已经统一到同一套公开契约。

从代码和演示角度看，本次迭代已经基本达到“细化迭代 3”的功能要求；从最终课程提交角度看，还需要继续整理 UML 终稿、部署说明和答辩材料，使报告文档完整覆盖第三次迭代成果。
